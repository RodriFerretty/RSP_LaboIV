/*
(Productos) Crear un formulario para cargar productos con los siguientes campos: 
nombre, marca, stock, tipo, local y precio (validar los datos).
*/
export class Product {
    public uid: string;
    public nombre: string;
    public marca: string;
    public stock: number;
    public tipo: string;
    public local: string;
    public precio: number;
}